import OpenAI from "openai";

const openai = new OpenAI({
  apiKey: process.env.OPENAI_API_KEY,
});

export async function POST(req: Request) {
  try {
    const body = await req.json();

    const outputType = body.outputType ?? "report";

    const input = `
案件名：
${body.projectName ?? ""}

作成したいもの：
${outputType === "segment" ? "セグメント用データ" : "レポート用データ"}

名称：
${body.reportName ?? ""}

目的：
${body.reportPurpose ?? ""}

見たいこと / 作りたい条件：
${body.whatToSee ?? ""}

指標定義 / 判定定義：
${body.metricDefinition ?? ""}

利用するデータ：
${body.dataFiles ?? ""}

利用期間：
${body.period ?? ""}

除外条件：
${body.excludeConditions ?? ""}

確認事項への回答：
${body.answeredQuestions ?? ""}

自動取得サンプルデータ：
${body.sampleDataFromMaster ?? ""}
`;

    const commonRule = `
あなたは、b→dashのデータ設計に詳しい設計支援AIです。
役割は、顧客や社内メンバーが「このデータをどう実装するか」を理解できるレベルの設計概要を作ることです。

■ 確認事項への回答を必ず優先すること
・【確認事項への回答】にある内容は設計前提として扱うこと
・回答済みのことは再度聞かないこと

■ 自動取得サンプルデータを必ず参照すること
・【自動取得サンプルデータ】に含まれるカラム名、粒度、サンプル値を必ず参照すること
・存在しないカラムを勝手に作らないこと
・利用するデータ、統合データ、出力列はサンプルデータを優先して判断すること

■ 統合データの基本思想
・最終統合データは、Excelのピボット集計で柔軟に集計できる形を優先すること
・集計済みレポートそのものではなく、レポート軸や切り口を後から変更できる粒度で設計すること
・顧客を識別する値（例：組合員CD、会員番号、顧客IDなど）は基本的に最終統合データに持たせること
・ただし、目的上不要、または粒度が崩れる場合のみ例外とすること
・統合データサンプルは、ピボット集計前提の明細寄りデータとして示すこと

■ b→dash制約を踏まえること
・IF文の中で演算しない
・四則演算は1回につき2項目まで
・集約には必ずグループキーが必要
・全体集約時は固定値1カラムを追加してグループキーに使う
・加工DF内で統合しない
・統合後は別DFとして加工する
・識別用コードは整数型に変換しない
・件数系は1フラグ合計ではなくユニークカウントで考える

■ 出力方針
・日本語のみ
・論理名のみ
・SQLや物理名の説明は禁止
・抽象的すぎる表現は禁止
・何のためにその加工 / 統合をするかが分かるように書くこと
・今回は細かいb→dash設定値までは不要
・まずは実装の全体像が伝わることを優先する

■ 確認事項の返し方
・不足情報がある場合は設計書ではなく確認事項のみ返す
・確認事項は最大5個
・質問型ではなく、提案型・確認型にすること
・「〜で問題ないですか？」を基本形にすること

■ 返却形式
以下のどちらかのJSONのみを返してください。
JSON以外の文章は一切つけないこと。

① 情報不足のとき
{
  "type": "question",
  "questions": [
    "確認事項1",
    "確認事項2"
  ]
}

② 情報が足りているとき
{
  "type": "design",
  "result": "設計本文"
}
`;

    const tableRule = `
■ テーブル出力の最重要ルール
・「レポートサンプル」「統合データサンプル」「出力サンプル」「出力データイメージ」は、必ず Markdown の表形式で出力すること
・絶対に縦並びの箇条書きにしないこと
・1行目にヘッダー、2行目に区切り線、3行目以降にサンプル行を出すこと
・表は最低3行のサンプル行を出すこと
・列見出しは横並びにすること
・値も1レコード1行で横並びにすること

正しい例：
| 組合員CD | 年月 | 受注企画回 | 注文金額 |
| -------- | ---- | ---------- | -------- |
| 10001 | 2023-01 | 企画回1 | 3000 |
| 10002 | 2023-01 | 企画回1 | 5000 |
| 10001 | 2023-02 | 企画回2 | 2000 |

誤った例：
年月
2023-01

受注企画回
企画回1

注文金額
3000
`;

    const verifyRule = `
■ 検証方法の最重要ルール
・検証方法は必ず「今回使用した大元データを、どう集計して、どの結果と一致確認するか」を明記すること
・「整合性を見る」「確認する」だけの抽象表現は禁止
・元データを最小加工した確認方法を優先すること

■ 検証方法は必ず以下の構造で書くこと

① 確認粒度
　xxx

② 使用する大元データ
　xxx

③ 元データでの集計方法
　・xxx
　・xxx

④ 突合方法
　・xxx
　・xxx

⑤ 中間検証
　・xxx
　・xxx

⑥ 最終検証
　・xxx
　・xxx

⑦ 異常値確認
　・xxx
　・xxx

■ 良い例
① 確認粒度
　受注企画回 × 組合員CD単位で確認する

② 使用する大元データ
　【汎用】宅配注文データ

③ 元データでの集計方法
　・宅配注文データを使い、受注企画回ごとに注文金額を合計する
　・組合員CD単位でも注文金額を集計し、顧客別の値を確認する

④ 突合方法
　・上記集計結果と、最終統合データの注文金額を比較する
　・受注企画回単位、組合員CD単位の両方で一致確認する
`;

    const formatRule = `
■ フォーマットルール
・文章のベタ書きは禁止
・必ず「見出し → 構造化 → インデント」で書くこと
・1行で長く書かないこと
・説明は必ず改行して分けること

■ インデントルール
・大項目：①②③ または ■
・中項目：全角スペース1つ下げる
・箇条書き：さらに全角スペース1つ下げる

■ 書き方例
① 項目
　説明
　・詳細1
　・詳細2
`;

    const flowSampleRule = `
■ データフロー強化ルール
・4章「データフロー図と各加工/統合の目的と概要」では、すべての加工 / 統合ステップに対して「出力データイメージ」を必ず書くこと
・出力データイメージは必ずMarkdownテーブルで書くこと
・最低3行のサンプルデータを出すこと
・加工前ではなく「そのSTEPを実行した後のデータ状態」を書くこと
・カラムはそのSTEPの結果を正確に表現すること

■ 書き方
加工：xxx
　目的：
　　xxx
　概要：
　　xxx

　出力データイメージ：
　| カラム1 | カラム2 | カラム3 |
　| ------- | ------- | ------- |
　| xxx | xxx | xxx |
　| xxx | xxx | xxx |
　| xxx | xxx | xxx |

統合：xxx × xxx
　目的：
　　xxx
　概要：
　　xxx

　出力データイメージ：
　| カラム1 | カラム2 | カラム3 | カラム4 |
　| ------- | ------- | ------- | ------- |
　| xxx | xxx | xxx | xxx |
　| xxx | xxx | xxx | xxx |
　| xxx | xxx | xxx | xxx |

■ NG
× 出力データイメージなし
× 箇条書きだけ
× 表ではない
× 粒度が曖昧な表
× 「加工後データ」ではなく元データをそのまま出す
`;

    const reportPrompt = `
${commonRule}

${tableRule}

${verifyRule}

${formatRule}

${flowSampleRule}

今回は「レポート用データ」の設計です。

design の result は、必ず以下の5章構成で出力してください。

1. アウトプットレポートイメージ
2. レポート作成に必要な統合データイメージ
3. 利用するデータ
4. データフロー図と各加工/統合の目的と概要
5. 検証方法

【1. アウトプットレポートイメージ】
① レポート目的
　xxx

② 分析定義
　xxx

③ レポートサンプル
　必ずMarkdownテーブルで出力すること

【2. レポート作成に必要な統合データイメージ】
① 統合データ名
　xxx

② 粒度
　基本は顧客識別子を含めること
　例：組合員CD × 受注企画回

③ 持つべきカラム一覧
　・xxx：xxx

④ レポート使用列
　・xxx

⑤ 生成用列
　・xxx

⑥ 統合データサンプル
　必ずMarkdownテーブルで出力すること
　集計済みレポートそのものではなく、ピボット集計できる明細寄りのデータにすること
　顧客識別子は基本的に含めること

【3. 利用するデータ】
① 使うデータファイル名
　・xxx

② 各データの役割
　・xxx

③ そのデータを何のために使うか
　・xxx

【4. データフロー図と各加工/統合の目的と概要】
・加工 / 統合 だけで書くこと
・「取込」は使わないこと
・枠囲みは不要
・矢印とインデントで流れが分かるように書くこと
・各STEPごとに必ず「目的」「概要」「出力データイメージ」を書くこと

【5. 検証方法】
・必ず verifyRule の構造で書くこと
・今回使用した大元データをどう集計し、最終統合データのどの列と一致確認するかを書くこと

【今回の入力】
${input}
`;

    const segmentPrompt = `
${commonRule}

${tableRule}

${verifyRule}

${formatRule}

${flowSampleRule}

今回は「セグメント用データ」の設計です。

design の result は、必ず以下の5章構成で出力してください。

1. セグメント出力イメージ
2. セグメント判定に必要な統合データイメージ
3. 利用するデータ
4. データフロー図と各加工/統合の目的と概要
5. 検証方法

【1. セグメント出力イメージ】
① セグメントの利用目的
　xxx

② 判定定義
　xxx

③ 出力サンプル
　必ずMarkdownテーブルで出力すること

【2. セグメント判定に必要な統合データイメージ】
① 統合データ名
　xxx

② 粒度
　基本は顧客識別子を含めること
　例：組合員CD単位

③ 持つべきカラム一覧
　・xxx：xxx

④ 判定使用列
　・xxx

⑤ 生成用列
　・xxx

⑥ 統合データサンプル
　必ずMarkdownテーブルで出力すること
　配信 / 抽出に使える顧客単位または顧客×時系列単位のデータにすること

【3. 利用するデータ】
① 使うデータファイル名
　・xxx

② 各データの役割
　・xxx

③ そのデータを何のために使うか
　・xxx

【4. データフロー図と各加工/統合の目的と概要】
・加工 / 統合 だけで書くこと
・「取込」は使わないこと
・枠囲みは不要
・矢印とインデントで流れが分かるように書くこと
・各STEPごとに必ず「目的」「概要」「出力データイメージ」を書くこと

【5. 検証方法】
・必ず verifyRule の構造で書くこと
・今回使用した大元データをどう集計し、最終統合データのどの列と一致確認するかを書くこと
・条件に合う / 合わないサンプル確認も含めること

【今回の入力】
${input}
`;

    const prompt = outputType === "segment" ? segmentPrompt : reportPrompt;

    const response = await openai.chat.completions.create({
      model: "gpt-4o-mini",
      messages: [
        {
          role: "system",
          content:
            "あなたはb→dashのデータ設計に強い設計エンジニアです。返却は必ずJSONのみで行ってください。",
        },
        {
          role: "user",
          content: prompt,
        },
      ],
    });

    const text = response.choices[0].message.content ?? "";

    let parsed;

    try {
      parsed = JSON.parse(text);
    } catch {
      parsed = {
        type: "design",
        result: text,
      };
    }

    return Response.json(parsed);
  } catch (error) {
    return Response.json(
      {
        type: "error",
        error: "API実行中にエラーが発生しました",
        detail: String(error),
      },
      { status: 500 }
    );
  }
}